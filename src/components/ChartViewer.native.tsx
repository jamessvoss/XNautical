/**
 * ChartViewer Native Component
 * 
 * This is a re-export for native platforms.
 * Native uses DynamicChartViewer with Mapbox GL.
 * The base ChartViewer.tsx is only for legacy/reference.
 */

import DynamicChartViewer from './DynamicChartViewer.native';

export default DynamicChartViewer;
